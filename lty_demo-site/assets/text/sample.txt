This is a demo text file for compression research.
The file intentionally repeats common patterns used in web projects.

<div class="card item item--primary">Hello Compressor</div>
<div class="card item item--primary">Hello Compressor</div>
<div class="card item item--primary">Hello Compressor</div>

function renderCard(title, subtitle) {
  return `<article class="card"><h3>${title}</h3><p>${subtitle}</p></article>`;
}

Compression targets:
- Repeated tags and attributes
- Repeated CSS selectors
- Repeated string templates
